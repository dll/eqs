{
  "name": "eqs",
  "version": "0.1.0",
  "private": true,
  "description": "工程快捷服务 (Engineering Quick Service)",
  "scripts": {
    "dev:client": "pnpm --filter @eqs/client dev",
    "dev:admin": "pnpm --filter @eqs/admin dev",
    "dev:server": "cd packages/server && go run cmd/server/main.go",
    "build:client": "pnpm --filter @eqs/client build",
    "build:admin": "pnpm --filter @eqs/admin build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  }
}
